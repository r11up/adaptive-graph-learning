# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# Outputs
results/
*.png
*.pt
*.ckpt

# Data — keep the directory, not its contents
data/*
!data/.gitkeep

# Source specification documents are deliberately NOT tracked: publishing
# them would disclose the full filing. Keep them local.
*.pdf

# Original exploratory notebook, superseded by src/qagta and
# examples/walkthrough.ipynb. Kept locally, not published.
QAE_*.ipynb

# OS
.DS_Store

# Encoded cohort caches (large, regenerable)
results/*.pt

# Manuscript sources — unpublished, kept local
paper-tex/

# Encoded caches inside timestamped run dirs
results/**/encoded.pt

# LaTeX build artefacts
paper-tex/*.aux
paper-tex/*.log
paper-tex/*.out
paper-tex/*.synctex.gz
paper-tex/*.fls
paper-tex/*.fdb_latexmk
paper-tex/main.pdf
